---
apiVersion: v1
kind: Service
metadata:
  name: longhorn-tailscale
  namespace: longhorn-system
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: longhorn-ui
  ports:
    - port: 80
      targetPort: 8000
