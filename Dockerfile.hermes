FROM nousresearch/hermes-agent:main

USER root

# Check what's available
RUN which python3 && python3 --version && apt-get update && apt-get install -y --no-install-recommends python3-pip && python3 --version

USER 1000
